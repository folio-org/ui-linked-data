import { monographInstanceModel } from './instance.model';
import { monographWorkModel } from './work.model';

export const monographModels = {
  instance: monographInstanceModel,
  work: monographWorkModel,
};

export default monographModels;
