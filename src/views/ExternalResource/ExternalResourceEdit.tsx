import { ExternalResourceLoader } from '@components/ExternalResourceLoader';

export const ExternalResourceEdit = () => {
  // TODO: initiate fetching of external resource via :resourceId param here
  // afterwards, conditionally return Edit view
  // if the external resource has been successfully fetched

  return <ExternalResourceLoader />;
};
